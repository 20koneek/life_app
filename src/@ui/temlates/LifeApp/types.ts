import { BodyProps } from '../../organisms/Body'
import { BottomProps } from '../../molecules/Bottom'
import { HeaderProps } from '../../molecules/Header'

export interface Props {
    body: BodyProps
    bottom: BottomProps
    header: HeaderProps
}